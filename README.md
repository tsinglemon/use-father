# use-father
开发组件库的基础环境

## 使用方法

```js
import {Foo, Input} from 'use-father';

export default function () {
  return (
    <div>
      <Foo size="large">hello, world</Foo>
      <Input/>
    </div>
  
  );
}
```
